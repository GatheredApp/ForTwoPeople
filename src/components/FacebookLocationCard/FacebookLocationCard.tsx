import type { MapLocation } from '../../types/MapLocation'
import { FacebookPostEmbed } from '../FacebookPostEmbed/FacebookPostEmbed'
export function FacebookLocationCard({location,onClose}:{location:Extract<MapLocation,{kind:'facebook'}>;onClose:()=>void}){return <aside className="buffet-card facebook-location-card"><button className="close-button" onClick={onClose} aria-label="Close location details">×</button><div className="eyebrow facebook-eyebrow">Facebook Group Review</div><h2>{location.name}</h2><p className="not-mullet">Not yet reviewed by Mullet Review</p><address>{location.address}<br/>{[location.city,location.state,location.postalCode].filter(Boolean).join(', ')}</address><section className="card-facebook"><h3>Facebook Group Reviews</h3>{location.facebookReviews.map(r=><FacebookPostEmbed key={r.id} review={r} name={location.name}/>)}</section><div className="actions"><a href={`https://www.google.com/maps/dir/?api=1&destination=${location.latitude},${location.longitude}`} target="_blank" rel="noreferrer">Get Directions ↗</a></div></aside>}
